# 2. 알고리즘 (의사 코드) (10점)

## 1. `main` 함수의 전체 흐름
(프로젝트의 전체 작동 로직을 의사 코드로 작성하세요.)

(예시 - 관리 프로그램의 경우)
```
시작
    랜덤 숫자 생성 함수 호출 → 정답 숫자 저장
    최대 시도 횟수 설정 (예: 10)
    
    반복 (게임이 성공 또는 실패할 때까지):
        사용자 입력 함수 호출 → 입력값 받기
        비교 및 힌트 제공 함수 호출 → "너무 큼/작음" 출력
        시도 횟수 증가
        입력 기록 저장

        만약 정답을 맞췄다면:
            게임 상태를 '성공'으로 변경하고 반복 종료

        만약 시도 횟수가 최대 횟수를 초과했다면:
            게임 상태를 '실패'로 변경하고 반복 종료
    반복 끝

    최종 결과 출력 (성공/실패 및 정답 공개)
종료


## 2. 핵심 기능 1: (예: 학생 등록 함수)
```
(핵심 기능 1의 논리적 절차를 작성하세요.)

시작
    랜덤 시드 초기화
    1~100 사이의 임의의 숫자를 생성
    생성한 숫자를 정답 변수에 저장
종료


## 3. 핵심 기능 2: (예: 학생 검색 함수)
```
시작
    사용자로부터 숫자 입력받기

    만약 입력값이 정수가 아니거나 범위(1~100)를 벗어난다면:
        "잘못된 입력입니다. 1~100 사이의 숫자를 입력하세요." 출력
        다시 입력받기 (유효한 값이 들어올 때까지 반복)

    유효한 입력값을 반환
종료

## 3. 핵심 기능 3: (예: 학생 검색 함수)
```시작
    만약 입력값이 정답과 같으면:
        "정답입니다!" 출력
        결과로 '성공'을 반환

    만약 입력값이 정답보다 크면:
        "너무 큽니다." 출력

    만약 입력값이 정답보다 작으면:
        "너무 작습니다." 출력

    결과로 '계속 진행'을 반환
종료
